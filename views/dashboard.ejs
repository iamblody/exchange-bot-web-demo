<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>ExchangeBot Admin Panel</title>
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
    rel="stylesheet"
  />
</head>
<style>
    footer {
            margin-top: 80px;
            padding: 10px 0;
            background: rgba(0, 0, 0, 0.2);
            color: #fff;
            font-size: 1rem;
            font-style: italic;
            text-align: center;
            border-radius: 0px;
            backdrop-filter: blur(5px);
        }

        footer p {
            font-size: 16px;
            margin: 0;
        }
</style>
<body class="bg-light">
  <div class="container py-4">
    <h1 class="mb-4 text-center">💱 ExchangeBot Admin Panel</h1>

    <!-- Alert -->
    <% if (message) { %>
      <div class="alert alert-info">
        <%= message %>
      </div>
    <% } %>

    <!-- Stats -->
    <div class="row mb-4">
      <div class="col-md-4">
        <div class="card shadow-sm">
          <div class="card-body">
            <h5 class="card-title">📅 Son Rapor</h5>
            <p class="card-text">
              <strong><%= lastReportDate || "Henüz yok" %></strong>
            </p>
          </div>
        </div>
      </div>

      <div class="col-md-4">
        <div class="card shadow-sm">
          <div class="card-body">
            <h5 class="card-title">📂 Rapor Sayısı</h5>
            <p class="card-text"><%= fileCount %> dosya</p>
          </div>
        </div>
      </div>

      <div class="col-md-4 text-center">
        <form action="/run" method="GET">
          <button type="submit" class="btn btn-success mt-4 w-100">🚀 Botu Çalıştır</button>
        </form>
        <a href="/settings"><button type="button" class="btn btn-success mt-4 w-100">⚙️ Ayarları Düzenle</button></a>
      </div>
    </div>

    <!-- Dosya Listesi -->
    <h4 class="mb-3">📁 Kayıtlı Raporlar</h4>
    <% if (files && files.length > 0) { %>
      <ul class="list-group">
        <% files.forEach(file => { %>
          <li class="list-group-item d-flex justify-content-between align-items-center">
            <%= file %>
            <a href="/data/<%= file %>" class="btn btn-outline-primary btn-sm" download>İndir</a>
          </li>
        <% }) %>
      </ul>
    <% } else { %>
      <p class="text-muted">Henüz kayıtlı dosya yok.</p>
    <% } %>
  </div>
  <footer>
        <p>© 2025 Copyright - Blody </p>
    </footer>
</body>
</html>
